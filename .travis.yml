language: python
python:
  - "2.7"
install:
  - apt-get install libgdal1-dev
  - pip install -r requirements.txt --use-mirrors
script: nosetests
notifications:
  email: false
  irc: "irc.freenode.net#okfn-dev"
